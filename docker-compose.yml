version: '3.8'

services:
  mongo:
    image: mongo
    container_name: nest-catalog-mongo
    ports:
      - 27017:27017
    volumes:
      - mongodbdata:/data/db


  mysqlsrv:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "testedb"
    ports:
      - "3306:3306"
    volumes:
      - mysqldbdata:/data/db
    networks:
      - mysql-compose-network

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    networks:
      - mysql-compose-network

networks: 
  mysql-compose-network:
    driver: bridge

volumes:
  mongodbdata:
  mysqldbdata: